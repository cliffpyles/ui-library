import React from 'react';
import {{blueprintInstance}} from './{{blueprintInstance}}';

export default {
  title: 'Components|Layout/{{blueprintInstance}}'
}

const props = {
};
export const Default = () => <{{blueprintInstance}} {...props}>child content</{{blueprintInstance}}>;